{
  "version": 2,
  "builds": [
    {
      "src": "metaverse/apps/frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/metaverse/apps/frontend/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/metaverse/apps/frontend/dist/$1"
    }
  ]
}
